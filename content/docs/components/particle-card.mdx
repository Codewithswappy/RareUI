---
title: Particle Card
description: An interactive card with particle explosion effect on hover, perfect for team member profiles.
---

import { Preview } from "@/components/mdx/preview";
import ParticleCard from "@/components/rareui/ParticleCard";
import { PropsTable } from "@/components/mdx/PropsTable";
import { UsageGuide } from "@/components/mdx/UsageGuide";
import { FeaturesList } from "@/components/mdx/FeaturesList";

<Preview
  code={`'use client'

import { useMemo, useState, useRef, useEffect } from 'react'
import { motion, AnimatePresence } from 'motion/react'
import { clsx, type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'

function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}

interface ParticleData {
  x: number
  y: number
  posX: number
  posY: number
  randX: number
  randY: number
  rotate: number
  scale: number
  delay: number
  blur: number
}

interface ParticleCardProps {
  name?: string
  role?: string
  bio?: string
  img?: string
  tags?: string[]
  cols?: number
  rows?: number
}

// ... rest of component code`}
  cliCommand="npx rareui add particle-card"
>
  <div className="[&_img]:m-0 [&_img]:p-0 [&_img]:block [&_h3]:m-0 [&_p]:m-0 [&_button]:block [&_button]:visible">
    <ParticleCard />
  </div>
</Preview>

<UsageGuide steps={[
  {
    title: "Import the component",
    code: `import ParticleCard from '@/components/rareui/ParticleCard';`
  },
  {
    title: "Use in your application",
    code: `export default function App() {
  return (
    <ParticleCard />
  );
}`
  }
]} />

<FeaturesList features={[
  {
    title: "Particle Explosion Effect",
    description: "Image breaks into animated particles on hover."
  },
  {
    title: "Smooth Transitions",
    description: "Framer Motion powered animations."
  },
  {
    title: "Dual-Sided Content",
    description: "Front shows preview, back shows detailed info."
  },
  {
    title: "Fully Customizable",
    description: "Control particle density, content, and styling."
  },
  {
    title: "Responsive Design",
    description: "Adapts to different screen sizes."
  },
  {
    title: "Performance Optimized",
    description: "Uses useMemo for particle calculations."
  }
]} />

## Examples

<UsageGuide title="Examples" steps={[
  {
    title: "With Custom Content",
    code: `<ParticleCard
  name="Jane Doe"
  role="Senior Developer"
  bio="Full-stack engineer with 8+ years building scalable applications."
  img="https://images.unsplash.com/photo-1494790108377-be9c29b29330"
  tags={['React', 'Node.js', 'TypeScript']}
/>`
  },
  {
    title: "Adjusting Particle Density",
    code: `<ParticleCard
  cols={30}  // More columns = smaller particles
  rows={36}  // More rows = denser effect
/>`
  },
  {
    title: "Team Grid",
    code: `const teamMembers = [
  { name: 'Alice Chen', role: 'CEO', tags: ['Leadership', 'Strategy'] },
  { name: 'Bob Smith', role: 'CTO', tags: ['Engineering', 'Architecture'] },
  { name: 'Carol Wang', role: 'Designer', tags: ['UI/UX', 'Branding'] },
];

export default function Team() {
  return (
    <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
      {teamMembers.map((member) => (
        <ParticleCard key={member.name} {...member} />
      ))}
    </div>
  );
}`
  }
]} />

## Customization

### Change Background Gradient

Modify the back content gradient in the component:

```tsx
className="bg-linear-to-br from-purple-600 to-blue-600"
// Change to:
className="bg-linear-to-br from-emerald-600 to-cyan-600"
```

### Adjust Animation Timing

Modify the motion transition values:

```tsx
transition={{
  duration: 1.2,  // Slower explosion
  delay: p.delay,
  ease: [0.25, 0.46, 0.45, 0.94],
}}
```

### Performance Tips

For better performance with many cards:

```tsx
// Reduce particle density
<ParticleCard cols={15} rows={18} />

// Or lazy load cards
import dynamic from 'next/dynamic';
const ParticleCard = dynamic(() => import('@/components/rareui/ParticleCard'));
```

<PropsTable props={[
  {
    name: "name",
    type: "string",
    default: '"Sam Jenkins"',
    description: "Name displayed on the card front"
  },
  {
    name: "role",
    type: "string",
    default: '"Product Designer"',
    description: "Role or title displayed"
  },
  {
    name: "bio",
    type: "string",
    default: '"Default bio..."',
    description: "Biography shown on card back"
  },
  {
    name: "img",
    type: "string",
    default: "Unsplash URL",
    description: "Background image URL"
  },
  {
    name: "tags",
    type: "string[]",
    default: "['UI/UX', ...]",
    description: "Array of skill/tag labels"
  },
  {
    name: "cols",
    type: "number",
    default: "20",
    description: "Particle grid columns (affects particle size)"
  },
  {
    name: "rows",
    type: "number",
    default: "24",
    description: "Particle grid rows (affects particle density)"
  }
]} />
